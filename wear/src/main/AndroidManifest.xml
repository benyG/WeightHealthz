<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name=".ForgeWearApplication"
        android:allowBackup="false"
        android:label="Forge"
        android:supportsRtl="true">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!--
            L'app montre n'est pas autonome : la source de vérité est la base Room du téléphone,
            et tous les appels sortants (Gemini, Calendar, Alexa) partent de `app`. La montre
            saisit et affiche, puis synchronise via la Data Layer (DEPLOYMENT.md §12).
            Déclarer `false` est aussi ce qui fait installer la montre depuis le téléphone.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <activity
            android:name=".ui.WeighInActivity"
            android:exported="true"
            android:label="Pesée"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".tile.WeighInTileService"
            android:exported="true"
            android:label="Pesée Forge"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>

            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@android:drawable/ic_menu_edit"
                tools:ignore="MissingTilePreview" />
        </service>

        <service
            android:name=".complication.WeightGapComplicationService"
            android:exported="true"
            android:label="Écart au poids cible"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>

            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT" />
            <!-- L'écart ne change qu'après une pesée : une mise à jour horaire suffit. -->
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="3600" />
        </service>

    </application>

</manifest>
